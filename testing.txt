Virtual environment: python3 -m venv venv && source venv/bin/activate && python -m pip install --upgrade pip

pip freeze > requirements.txt

pip install -r requirements.txt


python3 -m venv venv && source venv/bin/activate && python -m pip install --upgrade pip

python -m pip install --upgrade pip && pip install -r requirements.txt


pyenv install 3.14.0

ngrok http 8000

ngrok http 8000

ngrok http 8000

ngrok http 8000

ngrok http 8000



docker pull iruperth/looping:1.0.2

Docker compose en cualquier ordenador.

services:
  web:
    image: iruperth/looping:1.0.2
    ports:
      - "8000:8000"

